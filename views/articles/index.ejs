<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Markdown Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
</head>

<body>
    <div class="container mt-4 mb-4">
        <h1 class="mb-4">Blog Articles</h1>
        <a href="/articles/new" class="btn btn-success">New Article</a>

        <% articles.forEach(({_id, title, createAt, description, slug})=> { %>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title">
                        <%= title %>
                    </h4>

                    <div class="card-subtitle test-muted mb-2">
                        <%= createAt.toLocaleDateString() %>
                    </div>

                    <div class="card-test mb-2">
                        <%= description %>
                    </div>

                    <a href="articles/<%= slug %>" class="btn btn-primary">Read More</a>

                    <a href="articles/edit/<%= _id %>" class="btn btn-info">Edit</a>

                    <form action="/articles/<%= _id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>
            <% }) %>
    </div>
</body>

</html>